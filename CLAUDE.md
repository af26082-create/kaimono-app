# 買い物・支出管理アプリ

買いたいものを買い物リストに登録し、実際に買ったらチェックを入れると自動で支出として記録されるアプリ。
買い物リストを経由せず、直接支出として登録することもできる。

## 技術構成（守ること）

- React + TypeScript + Vite
- Next.js は使わない（App Router / Pages Router どちらも）
- 画面は1つ（上下2セクション: 買い物リスト / 支出）。ルーティングは使わない
- 将来的に画面を分ける必要が出たら react-router-dom を使う
- スタイリングは通常のCSS。Tailwind CSS は使わない
- 状態管理は useState / useContext の範囲。Redux などの外部ライブラリは使わない
- データ保存はまず localStorage を検討する。外部DB・認証は扱わない

## 進め方（守ること）

- 「全部作って」と言われても、1つの機能・1つのコンポーネント単位に分けて確認しながら進める
- 実装後に「変更したファイル名 / 変更内容 / なぜその実装にしたか」を簡潔に説明する
- エラーが出たら、いきなり直さず「原因は何か」を先に説明してから修正する
- 開発者はWebアプリ開発初心者。専門用語には簡単な説明を添える
- コードには、何をしているかを日本語のコメントで書く
- どのファイルのどこに追加・変更するかを明記する

## 実装の進め方（この順番を必ず守る）

1. Claude が実装方針だけを説明する。この時点ではコードを書かない
2. 開発者が自分でコードを書いてみる
3. 開発者が「OK」「書けた」と言う
4. Claude が書かれたコードを評価する
   - 良いところを具体的に挙げる
   - 間違っているところ / 動かないところ / 改善できるところを、
     理由とセットで指摘する
5. Claude が完成版を書き直す

開発者から求められない限り、2〜3 を飛ばして先にコードを書かないこと。
コードは C:\プログラミング練習\kaimono-app 内のファイルを直接読んで評価する。

## 環境メモ

- 場所: C:\プログラミング練習\kaimono-app
- Claude側のシェルからは npm レジストリに接続できない（403）。
  `npm install` などパッケージ取得が必要な作業は、開発者がVS Codeのターミナルで実行する。
